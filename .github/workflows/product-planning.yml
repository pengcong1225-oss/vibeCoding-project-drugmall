name: Product Planning Automation

on:
  workflow_dispatch:          # 手动触发
  schedule:
    - cron: '0 0 * * 1'       # 每周一 UTC 0 点自动运行

jobs:
  planning:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: sudo apt-get update && sudo apt-get install -y curl git

      # ========== 工具安装 ==========
      - name: Install Tools
        run: |
          curl -fsSL https://raw.githubusercontent.com/SkeneTechnologies/skene/main/tui/install.sh | bash
          npm i -g @anthropic-ai/claude-code
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      # ========== 环境变量配置 ==========
      - name: Configure DeepSeek Environment
        run: |
          echo "ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic" >> $GITHUB_ENV
          echo "ANTHROPIC_AUTH_TOKEN=${{ secrets.DEEPSEEK_API_KEY }}" >> $GITHUB_ENV
          echo "ANTHROPIC_MODEL=deepseek-v4-pro[1m]" >> $GITHUB_ENV
          echo "ANTHROPIC_DEFAULT_OPUS_MODEL=deepseek-v4-pro[1m]" >> $GITHUB_ENV
          echo "ANTHROPIC_DEFAULT_SONNET_MODEL=deepseek-v4-pro[1m]" >> $GITHUB_ENV
          echo "ANTHROPIC_DEFAULT_HAIKU_MODEL=deepseek-v4-flash" >> $GITHUB_ENV

      # ========== 强制确保所有标签存在 ==========
      - name: Ensure Labels Exist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 使用 --force 创建标签，如果已存在则不会报错
          for label in product-insight feature-request ai-fix ai-review-needed ai-failed; do
            echo "Ensuring label: $label"
            gh label create "$label" --color "ededed" --force 2>/dev/null || echo "Label $label may already exist or cannot be created, will attempt to use it anyway."
          done

      # ========== 步骤1：代码考古 ==========
      - name: 1. Code Archaeology (Skene)
        env:
          LLM_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          LLM_PROVIDER: deepseek
        run: |
          skene analyze || echo "Warning: Skene analysis failed, continuing..."

      # ========== 步骤2：需求感知 ==========
      - name: 2. Demand Sensing (gh + Claude)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Fetching issues..."
          gh issue list --state all --limit 100 \
            --json number,title,body,labels,comments,reactionGroups \
            > issues.json

          if [ ! -s issues.json ] || [ "$(cat issues.json)" = "[]" ]; then
            echo "# User Demand Map" > readyourusers-report.md
            echo "当前仓库暂无 Issue 数据。" >> readyourusers-report.md
          else
            cat > /tmp/demand_prompt.md << 'PROMPT'
          你是一名资深产品经理。请分析 issues.json 文件中的 GitHub Issues 数据。

          请按以下结构生成一份 **用户需求地图 (User Demand Map)**，并将最终结果写入 readyourusers-report.md 文件：

          1. **需求聚类**：将 Issues 分组，如“功能请求”、“Bug 修复”、“文档”、“性能优化”、“用户体验”等。
          2. **热度排序**：对每个聚类，根据 reactionGroups 中的反应总数、评论数量和参与者数量综合计算热度，并排出前 5 名。
          3. **上升趋势**：识别近期频繁出现或 interaction 突然增加的需求。
          4. **潜在机会**：指出被多次提及但尚未解决的痛点，或跨聚类之间的关联需求。

          请保持报告简洁，每个部分使用 Markdown 标题和列表。
          PROMPT

            claude -p "$(cat /tmp/demand_prompt.md)" \
              --allowedTools "Read" \
              --max-turns 20
          fi

      # ========== 步骤3：AI 规划 ==========
      - name: 3. AI Planning with Claude
        run: |
          echo "Merging analysis results..."
          echo "# Combined Brief" > combined_brief.md
          [ -f skene-context/growth-plan.md ] && cat skene-context/growth-plan.md >> combined_brief.md
          [ -f readyourusers-report.md ] && cat readyourusers-report.md >> combined_brief.md

          cat > /tmp/generate_prd_prompt.md << 'PROMPT'
          你是一个资深产品经理。请仔细阅读下面的合并简报，然后生成一套完整的产品文档。

          【合并简报】
          $(cat combined_brief.md)

          【任务】
          请严格按照以下结构生成一份 Markdown 格式的 PRD 文档，并将完整内容写入 product-docs/prd.md 文件：

          1. **产品愿景与目标**
          2. **用户故事**：列出 3~5 个核心用户故事，每个故事必须独立一行，格式严格为：
             - 作为[角色]，我希望[功能]，以便[目的]
             示例：- 作为买家，我希望按药品类别筛选商品，以便快速找到所需药品。
          3. **功能需求**
          4. **非功能需求**
          5. **验收标准**

          如果合并简报内容为空或不足以生成完整文档，请基于合理推测给出一个通用的 MVP PRD。
          请确保文档专业、可执行，并且直接写入文件。
          PROMPT

          claude -p "$(cat /tmp/generate_prd_prompt.md)" \
            --allowedTools "Read" \
            --max-turns 30

          [ -f "product-docs/prd.md" ] && echo "✅ PRD generated." || { echo "⚠️ PRD missing, creating placeholder"; mkdir -p product-docs; echo "# Placeholder PRD" > product-docs/prd.md; echo "Generation failed." >> product-docs/prd.md; }

      # ========== 步骤4：合并报告并派发 Issue ==========
      - name: 4. Combine Reports and Dispatch Issues
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 生成综合报告
          echo "# AI Product Analysis Report $(date +%Y-%m-%d)" > analysis_summary.md
          [ -f skene-context/growth-plan.md ] && echo "## Growth Opportunities (Skene)" >> analysis_summary.md && cat skene-context/growth-plan.md >> analysis_summary.md
          [ -f readyourusers-report.md ] && echo "## User Demand Map" >> analysis_summary.md && cat readyourusers-report.md >> analysis_summary.md
          [ -f product-docs/prd.md ] && echo "## Generated PRD (Claude)" >> analysis_summary.md && cat product-docs/prd.md >> analysis_summary.md

          # 从 PRD 中提取用户故事
          if [ -f "product-docs/prd.md" ]; then
            grep -E '^- 作为.*我希望.*以便' product-docs/prd.md > /tmp/stories.txt || true
            if [ -s /tmp/stories.txt ]; then
              echo "Found user stories, creating issues..."
              while IFS= read -r story; do
                [ -z "$story" ] && continue
                title=$(echo "$story" | sed 's/^- 作为//; s/，.*//; s/。.*//' | xargs)
                [ -z "$title" ] && title="AI 生成的功能请求"
                gh issue create \
                  --title "功能请求: $title" \
                  --body "来源：AI 产品规划分析\n\n完整用户故事：$story" \
                  --label "feature-request" || echo "Failed to create issue for story: $story"
              done < /tmp/stories.txt
            else
              echo "No user stories found, creating a single analysis issue."
              # 再次确保 product-insight 标签存在
              gh label create "product-insight" --force 2>/dev/null || true
              gh issue create \
                --title "🤖 AI Product Analysis Report $(date +%Y-%m-%d)" \
                --body "查看完整报告：[analysis_summary.md](./analysis_summary.md)" \
                --label "product-insight" || echo "Could not create summary issue, but continuing..."
            fi
          else
            echo "PRD file missing, creating a single analysis issue."
            gh label create "product-insight" --force 2>/dev/null || true
            gh issue create \
              --title "🤖 AI Product Analysis Report $(date +%Y-%m-%d)" \
              --body "PRD 生成失败，请检查工作流日志。" \
              --label "product-insight" || echo "Could not create summary issue."
          fi

      # ========== 步骤5：提交报告 ==========
      - name: 5. Commit Reports
        run: |
          git config user.name "Product Brain Bot"
          git config user.email "brain-bot@your-domain.com"
          git add analysis_summary.md product-docs/ skene-context/ readyourusers-report.md combined_brief.md 2>/dev/null || true
          git add issues.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "🤖 Automated product analysis and PRD $(date +%Y-%m-%d)"
            git push
          fi