# 医生端首页数据来源说明

##  问题描述

**用户问题**: "数据从哪来的，同时问诊的数据为什么没有读取"

**具体现象**:
1. 首页"今日待办"显示的是硬编码数据（3条待回复、2张待审核等）
2. 问诊数据实际上已经从数据库读取了，但没有正确显示在首页

---

##  问题分析

### 1. 首页"今日待办"数据来源

**文件**: `frontend-doctor/src/views/home/index.vue` (第21-26行)

```typescript
const todoItems = [
  { icon: 'warning', color: 'orange', title: '待回复患者咨询', count: '3条待回复', action: '去回复' },
  { icon: 'prescription', color: 'blue', title: '待审核处方', count: '2张待审核', action: '去审核' },
  { icon: 'file', color: 'gray', title: '待完善病历', count: '1份待完善', action: '去完善' },
  { icon: 'clock', color: 'red', title: '即将过期随访', count: '1个 2小时后', action: '去随访' }
]
```

**问题**: ❌ 这些是硬编码的固定数据，不会随数据库变化

---

### 2. 问诊数据实际已读取

**后端日志显示**:
```sql
SELECT * FROM dm_consultation WHERE doctor_id = 'DOC001'
```

**查询结果** (共10条记录):
| ID | 状态 | 费用 | 创建时间 |
|---|---|---|---|
| C001 | completed | 50.00 | 2024-03-20 |
| C004 | pending | 50.00 | 2024-03-20 |
| CONS001 | completed | 50.00 | 2024-03-20 |
| CONS007 | cancelled | 50.00 | 2024-03-19 |
| CONS1001 | pending | 19.90 | 2026-04-28 |
| CONS1002 | processing | 19.90 | 2026-04-28 |
| CONS1003 | completed | 19.90 | 2026-04-27 |
| CONS1778033151263 | pending | 19.90 | 2026-05-06 |
| CONS1778033650519 | cancelled | 19.90 | 2026-05-06 |
| CONS1778036285959 | pending | 19.90 | 2026-05-06 |

**当前统计**:
- ✅ pending (待接诊): 4条
- ✅ processing (接诊中): 1条
- ✅ completed (已完成): 3条
- ✅ cancelled (已取消): 2条

---

### 3. 数据流程分析

#### 后端API

**接口**: `GET /v1/doctor/todo-count`

**文件**: `backend/src/main/java/com/drugmall/service/impl/DoctorServiceImpl.java` (第154-166行)

```java
public DoctorTodoCountVO getTodoCount(String doctorId) {
    DoctorStatsVO stats = getStats(doctorId);
    DoctorTodoCountVO todoCount = new DoctorTodoCountVO();
    
    // 待办 = 待接诊 + 待审核处方
    int pendingConsultations = stats.getPending() != null ? stats.getPending() : 0;
    int pendingPrescriptions = countPendingPrescriptions(doctorId);
    todoCount.setTodoCount(pendingConsultations + pendingPrescriptions);
    
    // TODO: 从消息表获取未读消息
    todoCount.setUnreadCount(3);
    return todoCount;
}
```

**返回数据**:
```json
{
  "todoCount": 5,  // 待接诊(4) + 待审核处方(1)
  "unreadCount": 3  // 硬编码的未读消息数
}
```

---

#### 前端Store

**文件**: `frontend-doctor/src/stores/doctor.ts` (第94-98行)

```typescript
// 调用真实API获取待办数量
const todoRes = await getTodoCount()
if (todoRes.data) {
  todoCount.value = todoRes.data.todoCount || 0
  unreadCount.value = todoRes.data.unreadCount || 0
}
```

**存储的值**:
- `todoCount`: 5 (待办总数)
- `unreadCount`: 3 (未读消息)

---

#### 前端首页

**文件**: `frontend-doctor/src/views/home/index.vue`

**问题所在**:
1. ❌ 没有使用`doctorStore.todoCount`和`doctorStore.unreadCount`
2. ❌ 使用硬编码的`todoItems`数组
3. ✅ 只有头部消息图标使用了`doctorStore.unreadCount` (第85行)

---

## 📋 数据展示位置

### ✅ 正确显示数据的地方

1. **头部消息图标** (第85行)
```vue
<span v-if="doctorStore.unreadCount > 0" class="message-badge">
  {{ doctorStore.unreadCount }}
</span>
```

2. **数据概览卡片** (第95-100行)
```vue
<div class="stat-value">{{ doctorStore.todayStats.pending }}</div>  <!-- 待接诊 -->
<div class="stat-value">{{ doctorStore.todayStats.processing }}</div>  <!-- 接诊中 -->
<div class="stat-value">{{ doctorStore.todayStats.completed }}</div>  <!-- 已完成 -->
<div class="stat-value">{{ doctorStore.todayStats.income }}</div>  <!-- 今日收入 -->
```

### ❌ 未使用API数据的地方

1. **今日待办** (第21-26行) - 硬编码
2. **患者消息** (第28-32行) - 硬编码
3. **系统公告** (第34-38行) - 硬编码

---

## 🎯 需要改造的内容

### P0 - 高优先级

#### 1. 改造"今日待办"为真实数据

**后端新增API**:
```java
// GET /v1/doctor/todos
@GetMapping("/todos")
public Result<List<TodoItemVO>> getTodos() {
    // 返回详细的待办列表
}
```

**前端改造**:
```typescript
// 替换硬编码的todoItems
const todoItems = ref([])

onMounted(async () => {
  // 调用API获取真实待办
  const res = await getTodos()
  todoItems.value = res.data
})
```

---

#### 2. 改造"患者消息"为真实数据

**后端新增API**:
```java
// GET /v1/doctor/messages/recent
@GetMapping("/messages/recent")
public Result<List<MessageVO>> getRecentMessages() {
    // 返回最近的3条患者消息
}
```

**前端改造**:
```typescript
// 替换硬编码的patientMessages
const patientMessages = ref([])

onMounted(async () => {
  const res = await getRecentMessages()
  patientMessages.value = res.data
})
```

---

#### 3. 改造"系统公告"为真实数据

**后端新增API**:
```java
// GET /v1/system/notices
@GetMapping("/notices")
public Result<List<NoticeVO>> getNotices() {
    // 返回系统公告列表
}
```

**前端改造**:
```typescript
// 替换硬编码的notices
const notices = ref([])

onMounted(async () => {
  const res = await getNotices()
  notices.value = res.data
})
```

---

## 🔧 立即可用的修复方案

### 方案1: 使用现有API简单显示

**修改首页**:
```vue
<template>
  <div class="todo-section">
    <h3 class="section-title">今日待办</h3>
    <div class="todo-item" @click="goToPage('/consultation')">
      <div class="todo-icon warning">
        <svg>...</svg>
      </div>
      <div class="todo-info">
        <div class="todo-title">待回复患者咨询</div>
        <div class="todo-count">{{ doctorStore.todayStats.pending }}条待回复</div>
      </div>
      <div class="todo-action">去回复</div>
    </div>
  </div>
</template>
```

### 方案2: 完整改造（推荐）

需要新增3个后端API和对应的前端API模块，实现完整的数据驱动。

---

## 📊 数据库查询验证

### 查询待接诊数量
```sql
SELECT COUNT(*) FROM dm_consultation 
WHERE doctor_id = 'DOC001' AND status = 'pending';
```
**预期结果**: 4

### 查询待审核处方数量
```sql
SELECT COUNT(*) FROM dm_prescription 
WHERE doctor_id = 'DOC001' AND status = 'pending';
```
**预期结果**: 根据实际数据

### 查询问诊统计
```sql
SELECT 
  status,
  COUNT(*) as count,
  SUM(fee) as total_fee
FROM dm_consultation 
WHERE doctor_id = 'DOC001'
GROUP BY status;
```

---

## ✨ 当前状态总结

| 模块 | 数据来源 | 状态 | 需要改造 |
|------|----------|------|----------|
| 医生信息 | ✅ 真实API | 正常 | 不需要 |
| 数据概览 | ✅ 真实API | 正常 | 不需要 |
| 今日待办 | ❌ 硬编码 | 需改造 | 是 |
| 患者消息 |  硬编码 | 需改造 | 是 |
| 系统公告 | ❌ 硬编码 | 需改造 | 是 |
| 未读消息数 | ✅ 真实API | 正常 | 不需要 |

---

**报告时间**: 2026-05-06  
**状态**: 待实施改造
