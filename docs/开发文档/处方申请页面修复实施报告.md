# 处方申请页面修复实施报告

**日期**: 2026-04-28  
**状态**: ✅ 前端完成，⏳ 后端待实现

---

## 📋 问题分析

用户反馈：点击"凭方购买"跳转到申请处方药界面时，页面药品信息没有读取出来，所有硬编码信息应该从数据库读取，并在页面提交时保存在数据库中。

---

## ✅ 已完成的工作

### 1. 前端实现

#### 1.1 药品信息获取优化

**文件**: `frontend/src/views/prescription/apply.vue`

**修改内容**:
- ✅ 扩展`fetchDrugInfo`函数，获取完整的药品信息
- ✅ 包含所有新增字段：医保编码、追溯码、长处方标识、医保类别
- ✅ 包含规格列表（specifications）
- ✅ 设置默认选中的规格ID

**代码示例**:
```typescript
const fetchDrugInfo = async () => {
  const drugId = route.query.drugId as string
  if (drugId) {
    try {
      const drugDetail = await getDrugDetail(drugId)
      
      // 构建完整的药品信息对象，包含所有字段
      drugInfo.value = [{
        id: drugDetail.id,
        name: drugDetail.name,
        genericName: drugDetail.genericName || '',
        specification: drugDetail.specification || '',
        manufacturer: drugDetail.manufacturer || '',
        price: drugDetail.price,
        originalPrice: drugDetail.originalPrice,
        image: drugDetail.image || '',
        images: drugDetail.images || [],
        isRx: drugDetail.isRx || false,
        isNationalEssential: drugDetail.isNationalEssential || false,
        categoryId: drugDetail.categoryId || '',
        categoryName: drugDetail.categoryName || '',
        stock: drugDetail.stock || 0,
        sales: drugDetail.sales || 0,
        status: drugDetail.status || 1,
        // 医保相关字段
        medicalInsuranceCode: drugDetail.medicalInsuranceCode,
        traceabilityCode: drugDetail.traceabilityCode,
        isLongPrescription: drugDetail.isLongPrescription,
        insuranceCategory: drugDetail.insuranceCategory,
        // 规格列表
        specifications: drugDetail.specifications || [],
        // ... 其他字段
      }]
      
      // 设置默认选中的规格ID（如果有规格列表）
      if (drugDetail.specifications && drugDetail.specifications.length > 0) {
        const defaultSpec = drugDetail.specifications.find((s: any) => s.isDefault) || drugDetail.specifications[0]
        ;(drugInfo.value[0] as any).selectedSpecificationId = defaultSpec.id
        selectedSpecIndex.value = drugDetail.specifications.findIndex((s: any) => s.id === defaultSpec.id)
      }
      
      prescriptionStore.setSelectedDrugs(drugInfo.value)
    } catch (error) {
      console.error('获取药品信息失败:', error)
      ElMessage.error('获取药品信息失败')
    }
  }
}
```

#### 1.2 页面展示优化

**新增功能**:
- ✅ 显示追溯码（等宽字体、蓝色）
- ✅ 显示医保编码
- ✅ 显示医保类别（甲类绿色、乙类蓝色、丙类橙色）
- ✅ 显示长处方标识（绿色Tag）
- ✅ 支持多规格切换（如果有多个规格）
- ✅ 价格随规格变化

**模板代码**:
```vue
<!-- 医保信息 -->
<div v-if="drug.medicalInsuranceCode || drug.insuranceCategory || drug.isLongPrescription" class="medical-info">
  <div v-if="drug.traceabilityCode" class="info-item">
    <span class="label">追溯码:</span>
    <span class="value trace-code">{{ drug.traceabilityCode }}</span>
  </div>
  <div v-if="drug.medicalInsuranceCode" class="info-item">
    <span class="label">医保编码:</span>
    <span class="value">{{ drug.medicalInsuranceCode }}</span>
  </div>
  <div v-if="drug.insuranceCategory" class="info-item">
    <span class="label">医保类别:</span>
    <span class="value" :class="`category-${drug.insuranceCategory}`">
      {{ drug.insuranceCategory }}类
    </span>
  </div>
  <div v-if="drug.isLongPrescription" class="info-item">
    <span class="label">长处方:</span>
    <el-tag type="success" size="small" effect="plain">是</el-tag>
  </div>
</div>

<!-- 规格选择 -->
<div v-if="drug.specifications && drug.specifications.length > 1" class="spec-selection">
  <div class="spec-label">选择规格:</div>
  <div class="spec-options">
    <div
      v-for="(spec, index) in drug.specifications"
      :key="spec.id"
      :class="['spec-option', { active: selectedSpecIndex === index }]"
      @click="selectSpec(index)"
    >
      {{ spec.specName }}
    </div>
  </div>
</div>
```

#### 1.3 规格选择逻辑

**新增方法**:
```typescript
// 当前选中的规格索引
const selectedSpecIndex = ref(0)

// 当前选中的价格
const currentPrice = computed(() => {
  if (drugInfo.value.length === 0) return 0
  const drug = drugInfo.value[0]
  
  // 如果有规格列表，使用选中规格的价格
  if (drug.specifications && drug.specifications.length > 0) {
    const selectedSpec = drug.specifications[selectedSpecIndex.value]
    return selectedSpec?.price || drug.price
  }
  
  return drug.price
})

// 选择规格
const selectSpec = (index: number) => {
  selectedSpecIndex.value = index
  
  // 更新药品信息中的价格和规格
  if (drugInfo.value.length > 0 && drugInfo.value[0].specifications) {
    const spec = drugInfo.value[0].specifications[index]
    // 更新显示用价格（实际提交时会使用完整规格信息）
    drugInfo.value[0].price = spec.price
    drugInfo.value[0].specification = spec.specName
    // 保存选中的规格ID，用于提交时传递
    ;(drugInfo.value[0] as any).selectedSpecificationId = spec.id
  }
}
```

#### 1.4 样式优化

**新增样式**:
```scss
// 医保信息
.medical-info {
  margin-top: 8px;
  padding: 10px;
  background: #f5f7fa;
  border-radius: 8px;
  
  .info-item {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 6px;
    font-size: 12px;
    
    .label {
      color: $text-secondary;
      min-width: 60px;
      flex-shrink: 0;
    }
    
    .value {
      color: $text-primary;
      font-weight: 500;
      flex: 1;
      
      &.trace-code {
        font-family: 'Courier New', monospace;
        letter-spacing: 0.5px;
        font-size: 11px;
        color: #409eff;
      }
      
      &.category-甲类 { color: #52c41a; }
      &.category-乙类 { color: #1890ff; }
      &.category-丙类 { color: #faad14; }
    }
  }
}

// 规格选择
.spec-selection {
  margin-top: 8px;
  
  .spec-label {
    font-size: 12px;
    color: $text-secondary;
    margin-bottom: 6px;
  }
  
  .spec-options {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    
    .spec-option {
      padding: 6px 12px;
      border: 1px solid $border-color;
      border-radius: 16px;
      font-size: 12px;
      cursor: pointer;
      transition: all 0.2s ease;
      
      &.active {
        border-color: $primary;
        background: rgba($primary, 0.1);
        color: $primary;
        font-weight: 500;
      }
    }
  }
}
```

#### 1.5 API接口扩展

**文件**: `frontend/src/api/consultation.ts`

**修改内容**:
- ✅ 在`ConsultationApplyRequest`中添加`specificationId`字段

```typescript
export interface ConsultationApplyRequest {
  drugId: string
  specificationId?: number  // 规格ID
  patientId: number
  diseases: string
  symptoms?: string
}
```

#### 1.6 Store层优化

**文件**: `frontend/src/stores/prescription.ts`

**修改内容**:
- ✅ 在`submitPrescriptionApply`方法中传递规格ID
- ✅ 优先使用apply页面保存的选中规格ID

```typescript
const submitPrescriptionApply = async (): Promise<string> => {
  // ... 验证逻辑
  
  const selectedDrug = applyState.value.selectedDrugs[0]
  
  // 获取当前选中的规格ID（如果有）
  let specificationId: number | undefined
  if (selectedDrug.specifications && selectedDrug.specifications.length > 0) {
    // 优先使用apply页面保存的选中规格ID
    specificationId = Number((selectedDrug as any).selectedSpecificationId || selectedDrug.specifications[0].id)
  }
  
  // 调用真实API创建问诊记录
  const result = await apiApplyPrescription({
    drugId: selectedDrug.id,
    specificationId: specificationId,
    patientId: parseInt(applyState.value.selectedPatient.id),
    diseases: applyState.value.selectedDiseases.join(','),
    symptoms: applyState.value.symptoms
  })
  
  // ...
}
```

---

### 2. 后端准备

#### 2.1 DTO创建

**文件**: `backend/src/main/java/com/drugmall/dto/PrescriptionApplyDTO.java`

**内容**:
```java
package com.drugmall.dto;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;

/**
 * 处方药申请DTO
 */
@Data
@Schema(description = "处方药申请请求参数")
public class PrescriptionApplyDTO {

    @NotBlank(message = "药品ID不能为空")
    @Schema(description = "药品ID", required = true)
    private String drugId;

    @Schema(description = "规格ID")
    private Long specificationId;

    @NotNull(message = "患者ID不能为空")
    @Schema(description = "患者ID", required = true)
    private Integer patientId;

    @NotBlank(message = "疾病症状不能为空")
    @Schema(description = "疾病标签（多个用逗号分隔）", required = true, example = "感冒,发烧")
    private String diseases;

    @Schema(description = "症状描述")
    private String symptoms;
}
```

#### 2.2 Controller修改

**文件**: `backend/src/main/java/com/drugmall/controller/PatientConsultationController.java`

**修改内容**:
- ✅ 导入PrescriptionApplyDTO

---

## ⏳ 待完成的工作

### 后端API实现

需要在`PatientConsultationController`中添加处方申请的API端点：

```java
@PostMapping("/apply-prescription")
@Operation(summary = "申请处方药", description = "患者申请购买处方药")
public Result<Map<String, Object>> applyPrescription(
        @Valid @RequestBody PrescriptionApplyDTO applyDTO) {
    
    try {
        // 1. 验证药品是否存在
        Drug drug = drugMapper.selectById(Long.parseLong(applyDTO.getDrugId()));
        if (drug == null) {
            return Result.error("药品不存在");
        }
        
        // 2. 如果指定了规格ID，验证规格是否存在
        if (applyDTO.getSpecificationId() != null) {
            ProductSpecification spec = specificationMapper.selectById(applyDTO.getSpecificationId());
            if (spec == null) {
                return Result.error("药品规格不存在");
            }
        }
        
        // 3. 验证患者是否存在
        Patient patient = patientMapper.selectById(applyDTO.getPatientId());
        if (patient == null) {
            return Result.error("患者不存在");
        }
        
        // 4. 创建问诊记录
        Consultation consultation = new Consultation();
        consultation.setId("C" + System.currentTimeMillis());
        consultation.setPatientId(String.valueOf(applyDTO.getPatientId()));
        consultation.setPatientName(patient.getName());
        consultation.setType("prescription");
        consultation.setStatus("pending");
        consultation.setSymptom(applyDTO.getSymptoms());
        consultation.setIsRx(true);
        consultation.setDrugId(applyDTO.getDrugId());
        consultation.setSpecificationId(applyDTO.getSpecificationId());
        consultation.setDiseases(applyDTO.getDiseases());
        consultation.setCreateTime(LocalDateTime.now());
        
        consultationMapper.insert(consultation);
        
        // 5. 返回结果
        Map<String, Object> result = new HashMap<>();
        result.put("consultationId", consultation.getId());
        result.put("status", "pending");
        
        return Result.success(result);
        
    } catch (Exception e) {
        log.error("申请处方药失败", e);
        return Result.error("申请失败，请稍后重试");
    }
}
```

### 数据库表扩展

可能需要在`dm_consultation`表中添加以下字段：
- `drug_id` - 药品ID
- `specification_id` - 规格ID
- `diseases` - 疾病标签

---

## 📊 数据统计

### 前端修改
- **修改文件**: 3个
  - `frontend/src/views/prescription/apply.vue` (+143行)
  - `frontend/src/api/consultation.ts` (+1行)
  - `frontend/src/stores/prescription.ts` (+13行)

### 后端修改
- **新增文件**: 1个
  - `PrescriptionApplyDTO.java`
- **修改文件**: 1个
  - `PatientConsultationController.java` (+1 import)

---

## 🎯 功能特性

### 前端已实现
1. ✅ 从数据库获取完整药品信息
2. ✅ 显示追溯码、医保编码、医保类别、长处方标识
3. ✅ 支持多规格选择和切换
4. ✅ 价格随规格动态变化
5. ✅ 提交时传递规格ID到后端

### 后端待实现
1. ⏳ 处方申请API端点
2. ⏳ 验证药品和规格
3. ⏳ 创建问诊记录并保存药品信息
4. ⏳ 返回问诊ID供前端跳转

---

## 🔍 测试建议

### 前端测试
访问: `http://localhost:3003/prescription/apply?drugId=7`

**预期效果**:
1. ✅ 显示药品详细信息（名称、规格、厂家、价格）
2. ✅ 显示追溯码（蓝色等宽字体）
3. ✅ 显示医保编码
4. ✅ 显示医保类别（带颜色）
5. ✅ 显示长处方标识（如果是）
6. ✅ 显示规格选项（如果有多个）
7. ✅ 点击规格可切换，价格随之变化
8. ✅ 点击"提交申请"能正确传递数据

### 后端测试（待实现后）
```bash
curl -X POST http://localhost:8080/api/v1/patient/consultations/apply-prescription \
  -H "Content-Type: application/json" \
  -d '{
    "drugId": "7",
    "specificationId": 13,
    "patientId": 1,
    "diseases": "感冒,发烧",
    "symptoms": "头痛、发热"
  }'
```

**预期响应**:
```json
{
  "code": 200,
  "data": {
    "consultationId": "C1234567890",
    "status": "pending"
  }
}
```

---

## ✨ 总结

✅ **前端工作已全部完成**:
- 药品信息从数据库读取
- 医保信息完整展示
- 多规格支持完善
- 提交逻辑优化

⏳ **后端工作待完成**:
- 添加处方申请API端点
- 实现业务逻辑
- 保存到数据库

前端已经完全去除了硬编码，所有数据都从数据库读取，并且在提交时会正确传递药品信息和规格ID到后端！
